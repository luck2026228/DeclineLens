# 路线图

这里写的是**打算做的**，不是承诺。个人维护的项目，没有排期表，顺序也可能被真实需求打乱 —— 一条来自真实场景的「没收录的码」issue 的优先级，高于下面任何一项。

想推动某一项，最有效的办法是[开个讨论](https://github.com/luck2026228/DeclineLens/discussions)说说你的场景。**「我在真实业务里被这个卡住了」比「这个功能听起来挺好」有力得多。**

---

## 已经做完的

<sub>放在这儿是为了让下面的"没做"有个参照 —— 这个项目已经走过哪些阶段。</sub>

- **v1** 能抓到 `decline_code` 并显示
- **v2** 加字典翻译；插件版 + 油猴版双形态
- **v2.1** 删掉原始响应体存储（那 600 字节里可能夹着邮箱、姓名、账单地址）
- **v3** 加 `advice_code` 三色建议、`network_decline_code` 报文码、12 条报文规则兜底；零依赖自检
- **v3.1** 双版本共存握手；Firefox 走官方 MAIN 世界通道；自检加到 60 项
- **v3.1.2** 握手旗子挪到 DOM 上；补 Firefox `host_permissions`

---

## 下一步（近期）

### 1. 字典继续扩

**这是本项目最核心、也最需要外部帮忙的一件事。** 现在 55 条原因码 + 12 条报文规则，肯定有漏的。

Stripe 的码在持续增加，而且不同发卡行、不同地区触发的分布差别很大 —— 我一个人跑不出那么多组合。**你在真实场景里遇到一条没收录的，那条信息比我坐着翻文档翻出来的有价值得多。**

👉 [报一条没收录的码](https://github.com/luck2026228/DeclineLens/issues/new?template=missing-code.yml)

### 2. 上架应用商店

现在装扩展版要走「开发者模式 + 加载已解压」，对非技术用户门槛偏高。

- **Chrome Web Store** —— 一次性 5 美元开发者注册费，审核约 1～3 天。主要工作量在商店素材（描述、截图、隐私说明）
- **Firefox AMO** —— 免费，但审核对源码可读性要求高。这个项目零依赖、不压缩、不混淆，正好合它口味
- **Edge Add-ons** —— 免费，可以直接复用 Chrome 的包

上架之后油猴版依然保留，两条路并行。

### 3. 记录导出

现在只能「复制报告」（单条）。想要的是：把全部记录导出成 CSV 或 JSON，方便拿去做统计 —— 比如「这个月哪个原因码最多」。

约束：导出必须是**用户主动点击触发的本地文件下载**，不能有任何自动上传。

---

## 想做，但还没想清楚

### 4. 英文界面

README 已经有英文段落，但**界面本身是纯中文的**。做成双语要解决一个问题：语言从哪儿来？跟随浏览器语言最自然，但字典里的 `why` / `fix` 是本项目的核心资产，全部翻一遍工作量不小，而且翻得不好反而降低价值（`fix` 必须是一个能立刻执行的动作，直译会丢掉这一点）。

倾向的做法：先出英文版字典的骨架，让英文母语者来补，而不是我硬翻。

### 5. 支持别的支付网关

Adyen、Braintree、PayPal、Checkout.com 都有各自的拒付码体系，痛点是一模一样的。

但这件事有个硬门槛：**`PAY_URL` 那行正则就是本项目的隐私边界**（README 5.1 节）。每加一个网关就等于把读取范围扩大一次，必须一个域名一个域名地加，绝不能图省事写成通配。而且每个网关都要配一套独立字典 —— 工作量约等于重做一遍。

所以这件事只在有人真的需要、并且愿意一起维护那套字典的时候才做。

### 6. 「这个码最近多不多」的本地统计

面板里加一个小视图：按原因码分组，看趋势。纯本地计算，不发任何请求。

犹豫的点是这会让界面变复杂，而这个工具的价值恰恰在于**出问题时给你一句人话，然后消失**。加统计视图可能是往错误的方向长。

---

## 明确不做的

这几条是硬边界，写在 [CONTRIBUTING 第五节](CONTRIBUTING.md#五这个项目不想要什么)，这里再列一次省得反复问：

| 不做 | 为什么 |
| --- | --- |
| **任何网络请求** | 包括检查更新、匿名统计、错误上报。「零网络请求」是这个工具存在的前提，不接受例外 |
| **扩大权限** | Chrome 那边就一个 `storage`。不加 `tabs`、不加 `webRequest` |
| **存原始响应体** | v2.1 干过，里面可能夹着邮箱、姓名、账单地址、卡号后四位。已经删了，不会回来 |
| **引入依赖** | 零依赖是硬约束。没有依赖就没有供应链可以被投毒 |
| **自动重试支付** | 这个工具是只读旁听者。而且有些码（`lost_card` / `stolen_card` / `card_velocity_exceeded`）重试会让情况更糟 |
| **云端同步记录** | 记录只存在你自己浏览器里。要同步就要有服务器，有服务器就有了泄露面 |

---

## 版本节奏

- **补字典条目** → 补丁版本（3.1.x），攒够一批发一次
- **加功能 / 改界面** → 次版本（3.x.0）
- **改隐私边界、改权限、改存储结构** → 主版本（x.0.0），并且会在 CHANGELOG 顶部单独说明迁移影响

版本号只在 `manifest.json` 里写一次，其余全部由 `build.py` 推导 —— 这是本项目的两条「唯一来源」规则之一。
