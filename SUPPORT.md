# 遇到问题去哪儿问

先按这张表对号入座，能省你和我都省时间。

| 你的情况 | 去哪儿 |
| --- | --- |
| 装不上 / 浮球不出现 / 显示不对 | 先看下面的[常见问题](#常见问题)，八成在里面 |
| 看完还是不对，确定是坏了 | [开个 bug](https://github.com/luck2026228/DeclineLens/issues/new?template=bug-report.yml) |
| 遇到一条没收录的拒付码 | [报个码](https://github.com/luck2026228/DeclineLens/issues/new?template=missing-code.yml) —— **这是本项目最欢迎的贡献** |
| 想问「这个能不能……」「为什么这么设计」 | [讨论区](https://github.com/luck2026228/DeclineLens/discussions) |
| 想加功能 | 先去讨论区说说想干嘛，别直接写代码，免得白写 |
| 发现安全漏洞 | **不要开公开 issue**，走 [SECURITY.md](SECURITY.md) |
| 想自己改代码 | [CONTRIBUTING.md](CONTRIBUTING.md) 和 [ARCHITECTURE.md](ARCHITECTURE.md) |

---

## 常见问题

**付款成功的时候什么都不显示。**
设计如此。零记录时浮球完全隐藏，这个工具只在出问题的时候出现。

**浮球一直不出现。**
两种可能：这个站压根没触发拒付（那就是正常的）；或者钩子被站点的 CSP 挡了。后一种情况，**插件版弹窗顶部会有一条黄条**列出被挡的域名。看到黄条就换油猴版，油猴走的是另一条注入路径，不吃这个亏。

**抓到了但翻译得不对，或者是英文原文。**
说明这条码不在字典里，工具原样透传了 Stripe 的原文。这是刻意的，Stripe 没表态的时候我们不编。请用「没收录的原因码」模板报给我，我加进字典。

**装了它，我的支付会不会变慢或者失败？**
不会。它是旁听者：请求原样送走，响应 `clone` 一份自己看，原始那份原样交回页面。就算解析代码整段抛异常，也包在 `try/catch` 里，不影响支付流程。

**它能记录我在别的网站的什么行为吗？**
只有匹配 `PAY_URL` 那行正则的响应会被读取，其它请求它连看都不看。整个仓库没有一处对外发请求的代码，你可以自己 grep。正则本身见 [ARCHITECTURE.md](ARCHITECTURE.md#抓取范围-pay_url)。

**在 `checkout.stripe.com` 上能用吗？**
能。这也是为什么 `run_at` 必须是 `document_start`、`all_frames` 必须是 `true`，Stripe 自己的收银台和卡号输入框都是 iframe。

**记录会一直存着吗？占地方吗？**
最多 200 条，超了自动丢最旧的。每条就 11 个字段，200 条也才几十 KB。

**数据会同步到别的设备吗？**
不会。用的是 `storage.local` 不是 `storage.sync`，油猴版用 `GM_setValue`，全部只在本机。

---

## 报问题的时候带上这些

界面上有个**「复制报告」**按钮，点一下，粘进 issue 就行。它输出的是纯文本，只含 11 个诊断字段，**不含任何隐私信息**（完整字段清单见 [ARCHITECTURE.md](ARCHITECTURE.md#记录里存了哪-11-个字段)）。

手写的话，至少给这几样：

- 哪个版本（面板底部有版本号）
- 插件版还是油猴版
- 什么浏览器、什么版本
- 在哪类站点上（不用给真实域名，说「一个 Shopify 站」这种程度就够）
- 你期望看到什么，实际看到了什么

**请不要粘贴真实的卡号、姓名、账单地址、订单号或未脱敏的响应体。** 需要举例就用 Stripe 官方[测试卡号](https://stripe.com/docs/testing#declined-payments)。

---

## 我大概多久回

个人维护的项目，没有 SLA。实际情况：

- **没收录的原因码** —— 通常几天内加进字典，攒够一批发一个小版本
- **bug** —— 能复现的优先，复现不了的我会追问
- **功能请求** —— 先看是否符合 [CONTRIBUTING 第五节「这个项目不想要什么」](CONTRIBUTING.md#五这个项目不想要什么)，那里列了五条硬边界（零网络请求、不扩权限、不存原始响应体、零依赖、不自动重试）

超过两周没动静的话，在 issue 里 at 我一下，我可能是漏了。
